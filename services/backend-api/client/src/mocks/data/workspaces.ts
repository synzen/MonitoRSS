import { Workspace } from "@/features/workspaces";

const mockWorkspaces: Workspace[] = [
  { id: "workspace-1", name: "Acme Marketing", slug: "acme-marketing", role: "owner" },
  { id: "workspace-2", name: "Open Source Crew", slug: "open-source-crew", role: "admin" },
];

export default mockWorkspaces;
